$('#login-button').click(function (event) {
	var userName=document.getElementById("userName").value;  
    var pwd=document.getElementById("pwd").value;
    if(userName=="周周不想打工了" &&  pwd=="0324"){ 
		event.preventDefault();
		$('.avatar').fadeOut(500);
		$('form').fadeOut(500);
		$('.wrapper').addClass('form-success');

		fireConfetti();

		requestFullScreen();
		setTimeout(function(){location.href="index1.html";},5000);
		//auto_link_html("index1.html");
	}
	else{
		alert("密码是你的生日哦~（密码为4位数）");
	}
});

// 礼花效果
function fireConfetti() {
	var duration = 3 * 1000; // 持续时间 3 秒
	var end = Date.now() + duration;

	// 从左侧喷出
	(function frameLeft() {
		confetti({
			particleCount: 5,
			angle: 60,
			spread: 55,
			origin: { x: 0 }
		});
		if (Date.now() < end) {
			requestAnimationFrame(frameLeft);
		}
	})();

	// 从右侧喷出
	(function frameRight() {
		confetti({
			particleCount: 5,
			angle: 120,
			spread: 55,
			origin: { x: 1 }
		});
		if (Date.now() < end) {
			requestAnimationFrame(frameRight);
		}
	})();
}

function requestFullScreen(element) {
	var element=document.documentElement;
 // 判断各种浏览器，找到正确的方法
 //alert(element.webkitRequestFullScreen);
 var requestMethod = element.requestFullScreen || //W3C
 element.webkitRequestFullScreen || //Chrome等
 element.mozRequestFullScreen || //FireFox
 element.msRequestFullScreen; //IE11
 if (requestMethod) {
  requestMethod.call(element);
 }
 else if (typeof window.ActiveXObject !== "undefined") {//for Internet Explorer
  var wscript = new ActiveXObject("WScript.Shell");
  if (wscript !== null) {
   wscript.SendKeys("{F11}");
  }
 }
}
function changeFrameHeight(){
    var ifm= document.getElementById("iframepage"); 
    ifm.height=document.documentElement.clientHeight;

}

window.onresize=function(){  
     changeFrameHeight();  

} 